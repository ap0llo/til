= Clone a git repository with submodules

* Option 1: Use `git clone --recursive` for cloning the repository
* Option 2: When the repository is already cloned (without submodules), use `git submodule update --init --recursive` to clone all submodules
